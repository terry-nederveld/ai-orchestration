{
  "name": "@overture/server",
  "version": "0.1.0",
  "private": true,
  "description": "Overture local control plane: service layer and loopback HTTP API",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --root ../.. --project @overture/server"
  },
  "dependencies": {
    "@overture/core": "workspace:*",
    "@overture/orchestrator": "workspace:*",
    "@overture/workflow": "workspace:*"
  },
  "devDependencies": {
    "@overture/persistence": "workspace:*",
    "@overture/testkit": "workspace:*",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
