{
  "name": "@overture/experiments",
  "version": "0.1.0",
  "private": true,
  "description": "Experiment loop (§13–§15): hypothesis → candidates → prototype → rubric evaluation → judgment, as a resumable state machine",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run --root ../.. --project @overture/experiments"
  },
  "dependencies": {
    "@overture/core": "workspace:*",
    "@overture/workflow": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
